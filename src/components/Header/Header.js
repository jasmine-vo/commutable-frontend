import React from 'react';
import './Header.css';

const Header = () => (
  <header className="header-container">
    <h1 className="header-title">Commutable</h1>
  </header>
);

export default Header;